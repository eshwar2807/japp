[pytest]
markers =
    live: reaches the network. Excluded from the default run; use `-m live`.

# The default run must stay hermetic and fast. Network-backed checks exist to
# confirm a real employer page still behaves as expected, which is worth doing
# deliberately and not on every run.
addopts = -m "not live"
